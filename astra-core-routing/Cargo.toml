[package]
name = "astra-core-routing"
version = "0.1.0"
edition = "2024"
description = "Astra-core routing package — traffic routing rules"
license = "MIT"

[dependencies]
ipnetwork = { workspace = true }
regex = "1"
